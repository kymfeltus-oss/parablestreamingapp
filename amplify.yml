version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
        - pip3 install -r requirements.txt
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
# The cache section is intentionally REMOVED to force a clean build
# The old cache section was:
# cache:
#   paths:
#     - node_modules/**/*